"use client"; import { useState } from "react"; import { HiddenObject } from "./HiddenObject"; import { Key } from "./Key"; import { Door } from "./Door"; import { BirthdayReveal } from "./BirthdayReveal"; import { gameCopy } from "@/data/game";
export function GameBoard() { const [found,setFound]=useState(false); const [open,setOpen]=useState(false); return <section className="card"><h2>{gameCopy.title}</h2><Key found={found}/><div style={{display:"flex",gap:12,marginTop:20}}><HiddenObject found={found} onFind={()=>setFound(true)}/><Door canOpen={found} onOpen={()=>setOpen(true)}/></div><BirthdayReveal open={open} message={gameCopy.reveal}/></section>; }
